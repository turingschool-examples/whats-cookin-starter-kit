import { expect } from 'chai';
import recipeUtils from '../src/recipeUtils';

describe('RecipeUtils', () => {
  it('Should be a function', () => {
    expect(RecipeRepository).to.be.a('function');
  });
})