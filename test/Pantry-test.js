import { expect } from 'chai';
import Pantry from '../src/classes/Pantry';

beforeEach(function() {

});

describe('Pantry', () => {
    it('should be a function', function() {
        expect(Pantry).to.be.a('function');
      })

      it('should be able to tell the user they have enough ingredients for a given recipe', function() {
        expect;
      })

      it('should be able to tell the user they do not have enough ingredients for a given recipe', function() {
        expect;
      })

      it('should be able to determine the amount of missing ingredients still needed to cook a given recipe', function() {
        expect;
      })

      it('should be able to show a user what ingredients exist inside the pantry', function() {
        expect;
      })

      it('should allow user to check the list of recipes to cook to see if there are enough ingredients', function() {
        expect;
      })

      it('should be able to tell the user what ingredients are still needed if there are not enough in the pantry', function() {
        expect;
      })

      it('should', function() {
        expect;
      })

      it('should not allow a user to cook a recipe if there are not sufficient ingredients', function() {
        expect;
      })

      it('should not allow a user to cook a recipe if there are not sufficient ingredients', function() {
        expect;
      })

      it('should remove used ingredients from the pantry after cooking a recipe', function() {
        expect;
      })

      it('should allow a user to add more ingredients', function() {
        expect;
      })
})


// Methods
// Determine whether a user’s pantry has enough ingredients to cook a given recipe.
// Determine the amount of missing ingredients still needed to cook a given recipe, based on what’s in the user’s pantry.

// User Stories
// As a user, I should be able to view what ingredients exist inside of my pantry.
// As a user, I should be able to check my list of recipes to cook and see if my pantry has enough ingredients to cook a recipe.
// As a user, I should be told what ingredients are still needed if I don’t have enough ingredients in my pantry to cook the recipe.
// As a user, I should not be able to cook a recipe if I don’t have the ingredients required.
// As a user, when I cook a recipe, those ingredients should be removed from my pantry.
// As a user, I should be able to add more ingredients to my pantry.