class RecipeRepository {
  constructor() {
    //should hold on to all Recipe objects
    //should take in recipe data and create Recipe objects based on data (?)
  }

  //filterRecipesByTag() => will iterate through array of Recipes (this.recipes) and return new array of Recipes that match tag

  //filterRecipesByKeyword() => will iterate through this.recipes and return new array of Recipes that contain keyword in this.recipes.name OR this.recipes.ingredients.name

  //removeRecipe() => remove recipe from array of Recipe objects

  //addRecipe() => add recipe to array of Recipe objects
}

export default RecipeRepository;
