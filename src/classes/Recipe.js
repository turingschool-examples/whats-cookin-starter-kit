class Recipe {
  constructor() {
    //should take in an id, and it can set name, image, ingredients, instructions, and tags based on that id
    //idea -- function that finds recipe data based on id inputted and assigns name, image, ingredients, etc. of instance
  }

  //method getIngredientName() => determine the names of ingredients needed. Goes inside this.ingredients and iterates through
    // to return each one

  //method getIngredientCost() => iterate through this.ingredients and returns cost of each one

  //method returnInstructions() => iterate through this.instructions and return the text of each one
}

export default Recipe;
