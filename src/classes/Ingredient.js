class Ingredient {
  constructor() {
    //should take in an id that tells us its name and estimatedCostInCents based on ingredients.js
    //should take in a quantity object that tells us amount and unit
    //function finds id in ingredients file and assigns other properties based on that
  }
}

export default Ingredient;
